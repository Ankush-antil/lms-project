name: lms-portal
region: blr

services:
  - name: api
    github:
      repo: Ankush-antil/lms-project
      branch: main
      deploy_on_push: true
    source_dir: server
    environment_slug: node-js
    run_command: node server.js
    build_command: npm install
    http_port: 8080
    instance_size_slug: basic-xxs
    instance_count: 1
    routes:
      - path: /api
      - path: /uploads
    envs:
      - key: NODE_ENV
        value: "production"
        scope: RUN_AND_BUILD_TIME
      - key: PORT
        value: "8080"
        scope: RUN_TIME
      - key: MONGO_URI
        value: "mongodb+srv://digitalstudy:digitalstudy%23%405555@digitalstudy.lzqs6z8.mongodb.net/digitalstudy?retryWrites=true&w=majority&appName=Digitalstudy"
        type: SECRET
        scope: RUN_TIME
      - key: JWT_SECRET
        value: "LmsDeploy@d3b64c5eSecure!"
        type: SECRET
        scope: RUN_TIME
    health_check:
      http_path: /api/health

static_sites:
  - name: client
    github:
      repo: Ankush-antil/lms-project
      branch: main
      deploy_on_push: true
    source_dir: client
    build_command: npm install && npm run build
    output_dir: dist
    index_document: index.html
    error_document: index.html
    routes:
      - path: /
